# Tasks 009: Provider-Backed LLM Runtime

1. Confirm the existing graph and runtime tests that define the current `ModelTurnResult`, `ToolRequest`, approval, artifact, and append-only message invariants so Spec 009 preserves them instead of redefining them.
2. Add high-risk unit tests first for provider response translation, covering plain-text replies, allowed tool calls, malformed tool-call payloads, unknown tool names, excessive tool-call fan-out, and non-object tool arguments.
3. Add high-risk settings tests first for provider-backed mode selection, missing required credentials, invalid timeout or retry values, invalid max-tool-request settings, and explicit scaffold-mode fallback.
4. Add prompt-rendering tests before implementation drift can occur, proving the rendered prompt includes bounded transcript context, visible tools, approval guidance, attachment metadata already present in the manifest, and backend-authored response-format instructions.
5. Add the provider SDK dependency in `pyproject.toml` and extend `.env.example` plus `src/config/settings.py` with the explicit LLM runtime settings required by the spec, keeping secrets configurable and local scaffold mode explicit.
6. Refactor `src/providers/models.py` to preserve `ModelAdapter` and `RuleBasedModelAdapter` while adding one provider-backed adapter plus helper translation and failure-mapping functions that keep provider-native payloads inside the provider module boundary.
7. Update `apps/gateway/deps.py` to select the configured model adapter explicitly, fail clearly when provider mode is selected without required credentials, and keep deterministic scaffold mode available only through deliberate configuration.
8. Replace the current minimal prompt rendering in `src/graphs/prompts.py` with a structured backend-authored prompt builder that remains pure, testable, and independent of direct database access.
9. Make the smallest additive changes needed in `src/graphs/state.py` and `src/graphs/nodes.py` so visible tool metadata and bounded model-execution metadata can flow through the provider path without changing ownership of tool execution, approvals, assistant message persistence, or context manifest persistence.
10. Preserve deterministic handling in `src/policies/service.py` for `approve` and `revoke`, and ensure the provider-backed path cannot bypass exact-match approval enforcement for governed tools.
11. Wire provider failure mapping into the existing observability and run-failure path so timeouts, auth failures, rate limits, malformed responses, and provider unavailability remain classified, bounded, and compatible with worker retry behavior.
12. Add or extend runtime integration tests covering plain-answer turns, provider-suggested local-tool turns, provider-suggested approval-gated turns without active approval, provider-suggested approval-gated turns with exact active approval, and malformed provider responses.
13. Add failure-path integration coverage proving provider timeout and provider-unavailable scenarios do not corrupt append-only transcript or artifact state, surface the correct classified failure behavior, and remain observable through the existing Spec 008 seams.
14. Verify the final implementation keeps rule-based scaffold mode available by configuration, does not require live provider credentials for the default test suite, and does not introduce any direct provider dependency into repository, API, or channel-adapter code paths.
