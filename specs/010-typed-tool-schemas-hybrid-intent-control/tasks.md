# Tasks 010: Typed Tool Schemas and Hybrid Intent Control

1. Confirm the current tool, graph, provider, and policy seams in `src/tools/registry.py`, `src/graphs/nodes.py`, `src/providers/models.py`, and `src/policies/service.py` so Spec 010 tightens existing contracts instead of introducing a parallel execution path.
2. Add high-risk unit tests first for typed request-model validation covering `echo_text`, `send_message`, and `remote_exec`, including valid inputs, missing required fields, extra fields, wrong types, empty-string edge cases where relevant, and deterministic validation-error shapes.
3. Add high-risk canonicalization tests first for the shared argument-serialization path in `src/policies/service.py` or a new shared helper, proving equivalent validated inputs serialize identically, field order does not affect hashes, and schema-invalid inputs never produce approval-identity hashes.
4. Add high-risk policy tests first proving deterministic `approve <proposal_id>` and `revoke <proposal_id>` classification still bypasses provider interpretation, while non-administrative natural-language requests continue down the normal model path.
5. Add high-risk registry and provider-schema tests first proving each bound tool exposes one canonical schema definition that both runtime validation and provider-facing tool export consume, with no hard-coded `text` assumptions or prompt-only schema drift.
6. Extend `src/tools/registry.py` so `ToolDefinition` includes schema metadata, validation, canonicalization, and typed invocation hooks while preserving policy-filtered binding behavior.
7. Implement the typed request model and invocation changes in `src/tools/local_safe.py` so `echo_text` consumes validated input rather than parsing a raw dictionary inside the tool.
8. Implement the typed request model and invocation changes in `src/tools/messaging.py` so `send_message` uses validated input, preserves trimmed outbound-intent behavior for valid requests, and fails with bounded validation guidance for invalid ones.
9. Implement the typed request model and invocation changes in `src/tools/remote_exec.py` so approval lookup, canonical argument serialization, and node-runner invocation all consume canonical validated arguments, while infrastructure assertions such as missing runtime dependencies remain explicit runtime failures.
10. Refactor shared canonicalization in `src/policies/service.py` so approval lookup, exact-match enforcement, and proposal hashing operate on validated canonical arguments instead of raw untrusted dictionaries.
11. Update `src/graphs/nodes.py` so every tool request from deterministic or provider-backed paths is validated and canonicalized before approval checks, proposal creation, tool invocation, or append-only tool-event persistence.
12. Add the safe-completion path in `src/graphs/nodes.py` for schema-invalid tool requests so the assistant returns bounded guidance, observability records the validation failure, no tool executes, and schema-invalid governed requests do not create proposals.
13. Refactor provider tool-schema export in `src/providers/models.py` so provider-facing function definitions come from the canonical registry schema contract instead of prompt-only `argument_guidance` or hard-coded required-field logic.
14. Update `src/graphs/prompts.py` and any prompt-tool metadata helpers so human-readable tool guidance remains aligned with the typed schemas without becoming a second authoritative schema source.
15. Extend `src/sessions/repository.py` and `src/observability/audit.py` only as needed so append-only tool events, proposal packets, and audit records can persist bounded schema-validation metadata, canonical argument identity, and schema version without creating new business-state tables.
16. Add runtime tests proving schema-valid safe-tool requests still execute, schema-valid `send_message` requests still create outbound intents, schema-valid approved `remote_exec` requests still execute through the current governed path, and schema-valid but unapproved `remote_exec` requests still create proposals with canonical validated arguments.
17. Add failure-path tests proving malformed provider tool payloads, unavailable tool names, non-object arguments, and schema-invalid arguments all complete safely without execution, without accidental worker-retry inflation, and with bounded assistant-visible guidance.
18. Add backward-compatibility integration tests proving append-only transcript, tool-audit, proposal, approval, and outbound-intent flows remain intact after typed schema enforcement lands.
19. Finish with verification that provider-backed runtime still uses backend-owned schema definitions, deterministic administrative commands still bypass the model, and no tool execution path remains that accepts raw unvalidated arguments directly from provider output.
