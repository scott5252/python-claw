# Tasks 006: Remote Node Runner and Per-Agent Sandboxing

1. Add execution audit and sandbox-config schema if persisted.
2. Write tests for signature validation, allowlists, and sandbox-mode resolution.
3. Implement node-runner request validation and fail-closed denial paths.
4. Implement sandbox executor for `off`, `shared`, and `agent` modes.
5. Implement remote execution tool binding behind approval and policy checks.
6. Add execution audit logging and query surfaces.
7. Add integration tests for signed success, unsigned rejection, and denied commands.
